<div class="navbar navbar-default navbar-inverse-bg">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'BookWorm', root_path, class: 'navbar-brand' %>
    </div>
    <div class="nav navbar-right">
      <% if reader_signed_in? %>
        <%= link_to 'Logout', destroy_reader_session_path, method: :delete, class: 'btn btn-default navbar-btn' %>
      <% else %>
        <%= link_to 'Login', new_reader_session_path, class: 'btn btn-default navbar-btn' %>
      <% end %>
    </div>
  </div>
</div>
